name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  native-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build and test menu-bar app
        run: ./quota-overlay/build.sh
      - name: Build and test optional launcher
        run: ./launcher/build.sh
